export default function concatArrays(array1, array2, string) {
const array1 = ['a','b'];
const array1 = ['a','b'];

const combined = [...array1, ...array2];

const str = "hello";
const char = [...str];
}
